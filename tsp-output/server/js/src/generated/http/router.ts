// Generated by Microsoft TypeSpec

import * as http from "node:http";

import * as serverRaw from "./operations/server-raw.js";

import { parseHeaderValueParameters } from "../helpers/header.js";

import { Pages, Participants } from "../models/all/syncly-service.js";

import {
  RouterOptions,
  createPolicyChain,
  createPolicyChainForRoute,
  HttpContext,
} from "../helpers/router.js";

export interface SynclyServiceRouter {
  /**
   * Dispatches the request to the appropriate service based on the request path.
   *
   * This member function may be used directly as a handler for a Node HTTP server.
   *
   * @param request - The incoming HTTP request.
   * @param response - The outgoing HTTP response.
   */
  dispatch(request: http.IncomingMessage, response: http.ServerResponse): void;
}

export function createSynclyServiceRouter(
  pages: Pages,
  participants: Participants,
  options: RouterOptions<{
    pages: Pages<HttpContext>;
    participants: Participants<HttpContext>;
  }> = {},
): SynclyServiceRouter {
  const __onRequestNotFound_56 =
    options.onRequestNotFound ??
    ((ctx) => {
      ctx.response.statusCode = 404;
      ctx.response.setHeader("Content-Type", "text/plain");
      ctx.response.end("Not Found");
    });

  const __onInvalidRequest_57 =
    options.onInvalidRequest ??
    ((ctx, route, error) => {
      ctx.response.statusCode = 400;
      ctx.response.setHeader("Content-Type", "application/json");
      ctx.response.end(JSON.stringify({ error }));
    });

  const __onInternalError_58 =
    options.onInternalError ??
    ((ctx, error) => {
      ctx.response.statusCode = 500;
      ctx.response.setHeader("Content-Type", "text/plain");
      ctx.response.end("Internal server error.");
    });

  const __routePolicies_59 = options.routePolicies ?? {};

  const __routeHandlers_60 = {
    pages_create: createPolicyChainForRoute(
      "pagesCreateDispatch",
      __routePolicies_59,
      "pages",
      "create",
      serverRaw.pages_create,
    ),
    pages_read: createPolicyChainForRoute(
      "pagesReadDispatch",
      __routePolicies_59,
      "pages",
      "read",
      serverRaw.pages_read,
    ),
    pages_update: createPolicyChainForRoute(
      "pagesUpdateDispatch",
      __routePolicies_59,
      "pages",
      "update",
      serverRaw.pages_update,
    ),
    pages_delete: createPolicyChainForRoute(
      "pagesDeleteDispatch",
      __routePolicies_59,
      "pages",
      "delete",
      serverRaw.pages_delete,
    ),
    pages_confirm: createPolicyChainForRoute(
      "pagesConfirmDispatch",
      __routePolicies_59,
      "pages",
      "confirm",
      serverRaw.pages_confirm,
    ),
    pages_remind: createPolicyChainForRoute(
      "pagesRemindDispatch",
      __routePolicies_59,
      "pages",
      "remind",
      serverRaw.pages_remind,
    ),
    participants_list: createPolicyChainForRoute(
      "participantsListDispatch",
      __routePolicies_59,
      "participants",
      "list",
      serverRaw.participants_list,
    ),
    participants_create: createPolicyChainForRoute(
      "participantsCreateDispatch",
      __routePolicies_59,
      "participants",
      "create",
      serverRaw.participants_create,
    ),
    participants_read: createPolicyChainForRoute(
      "participantsReadDispatch",
      __routePolicies_59,
      "participants",
      "read",
      serverRaw.participants_read,
    ),
    participants_update: createPolicyChainForRoute(
      "participantsUpdateDispatch",
      __routePolicies_59,
      "participants",
      "update",
      serverRaw.participants_update,
    ),
    participants_delete: createPolicyChainForRoute(
      "participantsDeleteDispatch",
      __routePolicies_59,
      "participants",
      "delete",
      serverRaw.participants_delete,
    ),
  } as const;

  const dispatch = createPolicyChain(
    "SynclyServiceRouterDispatch",
    options.policies ?? [],
    async function (ctx, request, response) {
      const url = new URL(request.url!, `http://${request.headers.host}`);
      let path = url.pathname;

      if (path.length === 0) {
        return ctx.errorHandlers.onRequestNotFound(ctx);
      } else if (path.startsWith("/pages")) {
        path = path.slice(6);
        if (path.length === 0) {
          switch (request.method) {
            case "POST":
              return __routeHandlers_60.pages_create(ctx, pages);
            default:
              return ctx.errorHandlers.onRequestNotFound(ctx);
          }
        } else if (path.startsWith("/")) {
          path = path.slice(1);
          if (path.length === 0) {
            return ctx.errorHandlers.onRequestNotFound(ctx);
          } else {
            let __page_id_idx = path.indexOf("/");
            __page_id_idx = __page_id_idx === -1 ? path.length : __page_id_idx;
            const pageId = path.slice(0, __page_id_idx);
            path = path.slice(__page_id_idx);
            if (path.length === 0) {
              switch (request.method) {
                case "GET":
                  return __routeHandlers_60.pages_read(ctx, pages, pageId);
                case "PATCH":
                  return __routeHandlers_60.pages_update(ctx, pages, pageId);
                case "DELETE":
                  return __routeHandlers_60.pages_delete(ctx, pages, pageId);
                default:
                  return ctx.errorHandlers.onRequestNotFound(ctx);
              }
            } else if (path.startsWith("/")) {
              path = path.slice(1);
              if (path.length === 0) {
                return ctx.errorHandlers.onRequestNotFound(ctx);
              } else if (path.startsWith("confirmation")) {
                path = path.slice(12);
                if (path.length === 0) {
                  switch (request.method) {
                    case "PUT":
                      return __routeHandlers_60.pages_confirm(
                        ctx,
                        pages,
                        pageId,
                      );
                    default:
                      return ctx.errorHandlers.onRequestNotFound(ctx);
                  }
                } else {
                  return ctx.errorHandlers.onRequestNotFound(ctx);
                }
              } else if (path.startsWith("reminders")) {
                path = path.slice(9);
                if (path.length === 0) {
                  switch (request.method) {
                    case "POST":
                      return __routeHandlers_60.pages_remind(
                        ctx,
                        pages,
                        pageId,
                      );
                    default:
                      return ctx.errorHandlers.onRequestNotFound(ctx);
                  }
                } else {
                  return ctx.errorHandlers.onRequestNotFound(ctx);
                }
              } else if (path.startsWith("participants")) {
                path = path.slice(12);
                if (path.length === 0) {
                  switch (request.method) {
                    case "GET":
                      return __routeHandlers_60.participants_list(
                        ctx,
                        participants,
                        pageId,
                      );
                    case "POST":
                      return __routeHandlers_60.participants_create(
                        ctx,
                        participants,
                        pageId,
                      );
                    default:
                      return ctx.errorHandlers.onRequestNotFound(ctx);
                  }
                } else if (path.startsWith("/")) {
                  path = path.slice(1);
                  if (path.length === 0) {
                    return ctx.errorHandlers.onRequestNotFound(ctx);
                  } else {
                    let __participant_id_idx = path.indexOf("/");
                    __participant_id_idx =
                      __participant_id_idx === -1
                        ? path.length
                        : __participant_id_idx;
                    const participantId = path.slice(0, __participant_id_idx);
                    path = path.slice(__participant_id_idx);
                    if (path.length === 0) {
                      switch (request.method) {
                        case "GET":
                          return __routeHandlers_60.participants_read(
                            ctx,
                            participants,
                            pageId,
                            participantId,
                          );
                        case "PATCH":
                          return __routeHandlers_60.participants_update(
                            ctx,
                            participants,
                            pageId,
                            participantId,
                          );
                        case "DELETE":
                          return __routeHandlers_60.participants_delete(
                            ctx,
                            participants,
                            pageId,
                            participantId,
                          );
                        default:
                          return ctx.errorHandlers.onRequestNotFound(ctx);
                      }
                    } else {
                      return ctx.errorHandlers.onRequestNotFound(ctx);
                    }
                  }
                }
              }
            }
          }
        }
      }

      return ctx.errorHandlers.onRequestNotFound(ctx);
    },
  );

  const __errorHandlers_61 = {
    onRequestNotFound: __onRequestNotFound_56,
    onInvalidRequest: __onInvalidRequest_57,
    onInternalError: __onInternalError_58,
  };
  return {
    dispatch(request, response) {
      const ctx = { request, response, errorHandlers: __errorHandlers_61 };
      return dispatch(ctx, request, response).catch((e) =>
        __onInternalError_58(ctx, e),
      );
    },
  };
}
